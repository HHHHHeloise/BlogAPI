{
    "name": "blogapi",
    "version": "1.0.0",
    "description": "A full-stack JavaScript application with Node.js and React",
    "scripts": {
        "start": "cd client/blogapi && react-scripts --openssl-legacy-provider start",
        "heroku-postbuild": "cd client/blogapi && npm install && npm run build"
    },
    "engines": {
        "node": "18.x",
        "npm": "6.x"
    },
    "license": "ISC"
}
